describe("switchLetter", function() {
   it("It should switch every vowel for a dash.", function() {
     expect(switchLetter("switch your vowel for a dash.")).to.equal("sw-tch ---r v-w-l f-r - d-sh.");
   });
});
